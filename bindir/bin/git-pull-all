#!/opt/homebrew/bin/bash

dirs=$(fd -H -t d -g '.git')
#echo $dirs

for dir in $dirs; do (cd "$dir/.." && echo "--------------------------------------------------------" && echo $(pwd) && git pull); done
